{
  "name": "acolhimento-sistema",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gestão de Acolhimento Hospitalar — deploy único (server serve o client compilado)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm run build --prefix server",
    "seed": "npm run seed --prefix server",
    "start": "npm start --prefix server"
  }
}
